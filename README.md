# Certorix_Public
*Certorix provides an API-first platform for embedded diagnostic trees (JSON schema + HMAC signing) and verifiable product claims (semantic triples + ZKP). Includes React builder UI, vanilla JS widget, MCP plugin for AI agents, and OpenAPI 3.0 spec.*
